# Build PDF & ePub
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

formats:
  - epub
  - pdf

python:
  install:
    - requirements: docs/handbook/sources/requirements.txt

sphinx:
  configuration: docs/handbook/sources/conf.py
